#   Definitions for the fxns required to calculate the area and circ.
#   of a rectangle.
#   Send result back to user.


def area(length, width):
    #   fxn to calculate area
    return length * width


def perimeter(length, width):
    # fxn to calculate perimeter
    return 2 * (length + width)
